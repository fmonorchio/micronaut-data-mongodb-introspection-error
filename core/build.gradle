plugins {
    id("io.micronaut.minimal.library") version "3.4.1"
}

dependencies {
    api("io.micronaut:micronaut-jackson-databind")
    api("io.micronaut.data:micronaut-data-mongodb")
    api("io.micronaut.groovy:micronaut-runtime-groovy")
    api("org.mongodb:mongodb-driver-sync")
}

micronaut {
    processing {
        incremental(true)
        annotations("com.example.*")
    }
}
